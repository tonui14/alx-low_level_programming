#include "main.h"

/**
 * main - Entry function of the program
 * This function is the entry point of the program that prints a quote by Kevin
 * and returns 0 to indicate successful execution
 *
 * Return: Always 0 (Success)
 *
 **/

int main(void)
{
	_puts("\"At the end of the day, my goal was to be the best hacker\"\n"
		"\t- Kevin Mitnick");
	return (0);
}
