#include "main.h"
#include<stdio.h>
void _puts(char *str);
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void _puts(char *s)
{
	puts("\"Programming is like building a multilingual puzzle");
}
